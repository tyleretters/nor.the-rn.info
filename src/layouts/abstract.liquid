<!doctype html>
<html lang="en">
  <head>
    {% render metaTags, site: site %}
    <link rel="stylesheet" href="/styles.css?ver={% getTimestamp %}">
    <title>{% getTitle title %}</title>
  </head>
  <body>
    {{ content }}
  </body>
</html>
